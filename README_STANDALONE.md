# BizzWay Rider & Services (standalone project)

Copied from the main `bizzway` app; this folder combines **rider + home services** in one app.

## Run

```bash
cd bizzway_rider_services
flutter pub get
flutter run
```

Android package id: `com.saas.bizzway.rider_services`
